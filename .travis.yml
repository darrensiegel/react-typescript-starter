dist: trusty
sudo: required

language: ruby

services:
  - docker

before_install:
  - sudo apt-get update
  - sudo apt-get install docker-engine

before_script:
  - docker --version
  - docker-compose up -d --build
  - docker ps -a

script:
  - docker exec starter jest

after_script:
  - docker-compose down